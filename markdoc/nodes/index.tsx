import { Fence } from '@components/shared';

const nodes = {
  fence: {
    render: Fence,
    attributes: {
      language: {
        type: String
      }
    }
  }
};

export default nodes;
